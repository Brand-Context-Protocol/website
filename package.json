{
  "name": "bcp-website",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node scripts/sync-spec.mjs",
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "node scripts/sync-spec.mjs",
    "build": "astro build",
    "validate:bcp": "node scripts/validate-bcp.mjs",
    "validate:llms": "node scripts/validate-llms.mjs",
    "check:schema-links": "node scripts/check-schema-links.mjs",
    "check:bcp:live": "node scripts/check-live-bcp.mjs",
    "verify": "npm run validate:bcp && npm run validate:llms && npm run check:schema-links && npm run build && node scripts/check-favicon-fallback.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "devDependencies": {
    "astro": "^6.4.4"
  }
}
